language: bash

matrix:
  include:
    # Linux
    - os: linux
      dist: xenial
      script: python test/run.py
      env:
        - CC=clang 
        - CXX=clang++

    - os: linux
      dist: xenial
      script: python test/run.py
      env:
        - CC=clang 
        - CXX=clang++

    # macOS
    # - os: osx
    #   compiler: clang
    #   script: make test

    # # Windows
    # - os: windows
    #   env:
    #     - CMAKE_PATH="/c/Program Files/CMake/bin"
    #   script: 
    #     - export PATH=$CMAKE_PATH:$PATH
    #     - python test/run.py


